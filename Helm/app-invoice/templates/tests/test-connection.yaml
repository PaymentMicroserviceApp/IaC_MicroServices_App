apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-invoice.fullname" . }}-test-connection"
  labels:
    {{- include "app-invoice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-invoice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
