apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-pay.fullname" . }}-test-connection"
  labels:
    {{- include "app-pay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-pay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
