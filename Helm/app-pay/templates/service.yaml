apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-pay.fullname" . }}
  labels:
    {{- include "app-pay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-pay.selectorLabels" . | nindent 4 }}
