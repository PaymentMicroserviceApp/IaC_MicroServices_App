apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-transaction.fullname" . }}-test-connection"
  labels:
    {{- include "app-transaction.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-transaction.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
