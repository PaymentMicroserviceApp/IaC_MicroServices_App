apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-transaction.fullname" . }}
  labels:
    {{- include "app-transaction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-transaction.selectorLabels" . | nindent 4 }}
